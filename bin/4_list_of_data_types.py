"""
List of datatypes
"""

'''

IMMUTABLE TYPES
(Once we store the values, we CAN'T modify throught the program)
- numbers
- strings
- tuple
    -- to keep list of values
    -- it keeps duplicate values as well
- frozenset
    -- to keep list of values
    -- it keeps UNIQUE values only
- bytes


MUTABLE TYPES
(Once we store the values, we CAN modify throught the program)
- list
    -- to keep list of values
    -- it keeps duplicate values as well
- set
    -- to keep list of values
    -- it keeps UNIQUE values only
- bytearray

- dictionary
    -- to keep list of values
    -- it keeps duplicate values as well
    -- we are storing in key/value format

'''